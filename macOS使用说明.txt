===============================================
PubMed Papers Feed - macOS 使用说明
===============================================

📥 安装步骤
-----------------------------------------------

1. 解压下载的 ZIP 文件

2. 打开终端（Terminal），进入解压后的文件夹：
   cd ~/Downloads/PubMedPapersFeed

3. 给启动脚本添加执行权限（只需执行一次）：
   chmod +x start.command

4. 启动应用（两种方式）：

   方式 A - 双击打开：
   - 右键点击 start.command
   - 选择 "打开"
   - 首次运行会提示无法打开，点击 "取消"

   方式 B - 终端运行（推荐）：
   ./start.command

🔐 安全设置（首次运行必需）
-----------------------------------------------

由于应用未经过 Apple 官方签名，首次运行时：

1. 如果提示 "无法打开，因为无法验证开发者"
2. 前往：系统设置 > 隐私与安全性
3. 在 "安全性" 部分，找到 PubMedPapersFeed 的阻止记录
4. 点击 "仍要打开"
5. 再次运行 start.command

或者你可以在终端中运行（跳过安全限制）：
   xattr -cr PubMedPapersFeed
   ./start.command

⚙️ 配置 API Key
-----------------------------------------------

1. 首次运行会自动在 ~/.pubmed_papers_feed/ 创建 config.yaml
2. 编辑配置文件：
   open ~/.pubmed_papers_feed/config.yaml

3. 填写你的 API 密钥：
   llm:
     api_key: "your-api-key-here"
     model: "gpt-4"

4. 保存后重新运行 start.command

🌐 使用应用
-----------------------------------------------

1. 启动后会自动打开浏览器访问 http://localhost:8000
2. 如果浏览器没有自动打开，请手动访问上述地址
3. 在 Web 界面中配置兴趣主题、搜索参数
4. 开始搜索文献并生成文案

📁 数据存储位置
-----------------------------------------------

- 配置文件：~/.pubmed_papers_feed/config.yaml
- 数据库：~/.pubmed_papers_feed/data/pubmed.db
- 生成的报告：~/.pubmed_papers_feed/data/reports/

🛑 停止应用
-----------------------------------------------

在终端中按 Ctrl+C 即可停止服务

❓ 常见问题
-----------------------------------------------

Q: 提示 "Permission denied"
A: 运行 chmod +x start.command 添加执行权限

Q: 提示 "无法打开，因为 Apple 无法检查其是否包含恶意软件"
A: 前往 系统设置 > 隐私与安全性 > 点击"仍要打开"

Q: 端口 8000 被占用
A: 修改 main.py 中的端口，或使用 lsof -ti:8000 | xargs kill 关闭占用进程

Q: 如何查看日志？
A: 在终端中运行 ./start.command，所有日志会输出到终端

===============================================
技术支持
===============================================

如有问题，请访问项目主页提交 Issue：
https://github.com/yourusername/pubmed_papers_feed

===============================================
